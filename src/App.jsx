import { productos } from '../productos.json';
import { Component } from 'react';

const MiTabla = () => {
	const fetchData = () => {};
	return fetchData().map(producto => {
		console.log('caca');
		console.log('caca2');
	});
};

export default MiTabla;
